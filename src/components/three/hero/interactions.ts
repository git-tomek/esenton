/**
 * Node names (from hero.glb / HeroModel.generated.tsx) that react to pointer
 * events: they get a hover cursor + highlight and fire `onMeshClick`.
 * Add or remove names here to change what is clickable.
 */
export const INTERACTIVE_MESHES = new Set<string>([
  // 'monitor1',
  // 'monitor2',
  // 'iphone',
  // 'mug',
]);
